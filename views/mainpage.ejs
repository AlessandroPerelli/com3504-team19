<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/stylesheets/main.css">
  <title>Main</title>
</head>
<body>
  <% include components/navbar.ejs %>
  <div class="main-page-container flex">
    <h1>Explore plants</h1>
    <input type="text" id="search" class="search-input" placeholder="Search for a plant">
    <div class="category-links">
      <% categoryData.forEach(function(category) { %>
        <% var categoryName = category.categoryName; %>
        <% include components/categoryfilter.ejs %>
      <% }); %>
    </div>
    <% categoryData.forEach(function(category) { %>
      <% var categoryName = category.categoryName; %>
      <% var plants = category.plants; %>
      <% include components/category.ejs %>
    <% }); %>  </div>
</body>
</html>